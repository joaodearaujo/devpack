{
  "name": "devpack",
  "private": true,
  "version": "1.0.0",
  "description": "A local-first Linux development environment builder with an AI assistant.",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:frontend": "npm --workspace frontend run dev",
    "dev:backend": "npm --workspace backend run dev",
    "build": "npm --workspace frontend run build && npm --workspace backend run build",
    "check": "npm --workspace frontend run check && npm --workspace backend run check",
    "lint": "npm --workspace frontend run lint && npm --workspace backend run lint",
    "test": "npm --workspace frontend run test && npm --workspace backend run test"
  },
  "engines": {
    "node": ">=22"
  }
}
